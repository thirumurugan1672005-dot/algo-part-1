1 .Algorithms : Algorithms are the step by step instruction to execute a task<br/>
2. Runtime of an algorithm depends on number of operations as do as increase in input size <br/>
3 .Big O notation is widely used to determine runtime of an algorithm<br/>
4. We account only worst case scenario in algorithms because it tells this beyond the algorithm dont vary


<h1>Searching Algorithms</h1>
<p>Linear Search : Search one by one through iterable</p>
<p>Binary Search : Search in sorted array where from middle you keep on reducing search space until you find element</p>

<h1>Arrays</h1>
<ul>
  <li>They are indexed from zero to length of array - 1</li>
  <li>They are allowed to have random access</li>
  <li>
    Insertion and Deletion takes O(n) time complexity 
  </li>
  <li>
    Accessing Elements take O(1) time complexity
  </li>
  
</ul>
<h1>LinkedList</h1>
<ul>
  <li>bunch of memoru connected by pointers</li>
  <li>They followed only sequential access</li>
  <li>Insertion and deletion may takes the O(1) time complexity as know tail pointer</li>
  <li>Accessing elements will take O(1) elements </li>
</ul>
