1 .Algorithms : Algorithms are the step by step instruction to execute a task<br/>
2. Runtime of an algorithm depends on number of operations as do as increase in input size <br/>
3 .Big O notation is widely used to determine runtime of an algorithm<br/>
4. Arrays is datastructure which stores elements in continuous manner while linked lists store elements in chunk of memory<br/>
5. Arrays are indexed while linked list often stores memory of next element<br/>
6. Insertion and deletion : lists are perfect <br/>
7. Accessing and continous access : arrays are perfect<br/>


<h1>Searching Algorithms</h1>
<p>Linear Search : Search one by one through iterable</p>
<p>Binary Search : Search in sorted array where from middle you keep on reducing search space until you find element</p>
